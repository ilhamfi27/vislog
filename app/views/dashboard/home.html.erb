Hai <%= current_user.username %> |
<% if current_user.admin? %>
  <b>Admin</b>
<% else %>
  <b>User</b>
<% end %>
|
<%= link_to 'Log Out', destroy_user_session_path, method: :delete %>
<%= search_form_for @search, url: dashboard_path, method: :get do |f| %>
  <%= f.label :search_program, "Search Program" %>
  <%= f.search_field :program_cont, placeholder: "Search Program", class: "form-control" %>

  <%= f.submit :submit %>
<% end %>

